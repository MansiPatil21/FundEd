apiVersion: v1
kind: Namespace
metadata:
  name: funded
  labels:
    # Baseline rather than restricted: the official MongoDB and Nginx images start as root and
    # drop privileges themselves. The FundEd services run as non-root users regardless.
    pod-security.kubernetes.io/enforce: baseline
